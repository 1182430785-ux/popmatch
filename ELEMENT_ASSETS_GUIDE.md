# 元素资源使用指南

## 概述

PopMatch 现在支持两种元素渲染模式：
1. **颜色模式**：使用纯色块渲染元素（默认旧版）
2. **图片模式**：使用自定义图片/SVG 渲染元素（新功能）

---

## 渲染模式配置

在 `src/constants.ts` 中修改 `RENDER_MODE` 来切换渲染模式：

```typescript
export const RENDER_MODE: RenderMode = 'auto'; // 可选值：'color', 'image', 'auto'
```

### 模式说明

| 模式 | 说明 |
|------|------|
| `'color'` | 仅使用颜色渲染，不加载图片资源 |
| `'image'` | 仅使用图片渲染，如果图片加载失败会报错 |
| `'auto'` | **推荐** - 优先使用图片，如果图片不存在则回退到颜色模式 |

---

## 资源文件结构

```
public/
└── assets/
    └── elements/
        ├── element_0.svg    # 类型 0: 红色圆形
        ├── element_1.svg    # 类型 1: 青色方形
        ├── element_2.svg    # 类型 2: 黄色星形
        ├── element_3.svg    # 类型 3: 绿色三角形
        ├── element_4.svg    # 类型 4: 紫色菱形
        └── element_5.svg    # 类型 5: 橙色六边形
```

---

## 使用自定义图片

### 1. 准备图片资源

- **格式**：支持 PNG, JPG, SVG
- **尺寸**：推荐 60x60 像素（或更高清晰度，会自动缩放）
- **背景**：建议使用透明背景（PNG 或 SVG）
- **数量**：需要 6 张图片（对应 6 种元素类型）

### 2. 放置图片文件

将你的图片放到 `public/assets/elements/` 目录：

```bash
public/assets/elements/
├── element_0.png    # 你的第一种元素图片
├── element_1.png    # 你的第二种元素图片
├── element_2.png    # ...
├── element_3.png
├── element_4.png
└── element_5.png
```

### 3. 修改配置

如果使用不同的文件名或路径，在 `src/constants.ts` 中修改 `ELEMENT_CONFIGS`：

```typescript
export const ELEMENT_CONFIGS: ElementConfig[] = [
  {
    type: 0,
    color: 0xff6b6b,           // 备用颜色
    texture: 'my_element_0',   // Phaser 纹理的 key
    imagePath: 'assets/elements/my_element_0.png', // 实际文件路径
  },
  // ... 其他元素配置
];
```

### 4. 启动游戏

```bash
npm run dev
```

打开浏览器，图片会自动加载并显示！

---

## 示例：更换为自己的图片

### 使用水果主题

假设你有 6 张水果图片：

```
public/assets/fruits/
├── apple.png
├── banana.png
├── cherry.png
├── grape.png
├── lemon.png
└── orange.png
```

**修改 `src/constants.ts`：**

```typescript
export const ELEMENT_CONFIGS: ElementConfig[] = [
  {
    type: 0,
    color: 0xff6b6b,
    texture: 'fruit_apple',
    imagePath: 'assets/fruits/apple.png',
  },
  {
    type: 1,
    color: 0x4ecdc4,
    texture: 'fruit_banana',
    imagePath: 'assets/fruits/banana.png',
  },
  {
    type: 2,
    color: 0xffe66d,
    texture: 'fruit_cherry',
    imagePath: 'assets/fruits/cherry.png',
  },
  {
    type: 3,
    color: 0x95e1d3,
    texture: 'fruit_grape',
    imagePath: 'assets/fruits/grape.png',
  },
  {
    type: 4,
    color: 0xc7ceea,
    texture: 'fruit_lemon',
    imagePath: 'assets/fruits/lemon.png',
  },
  {
    type: 5,
    color: 0xffa07a,
    texture: 'fruit_orange',
    imagePath: 'assets/fruits/orange.png',
  },
];
```

---

## 故障排查

### 问题：图片不显示

**检查清单：**

1. ✅ 图片文件是否存在于 `public/` 目录下？
2. ✅ `imagePath` 路径是否正确？（相对于 `public/` 目录）
3. ✅ 图片文件名是否完全匹配（区分大小写）？
4. ✅ 打开浏览器控制台，查看是否有加载错误

**查看控制台日志：**

```
[MainScene] 开始加载元素图片资源...
[MainScene] 加载图片: element_0 <- assets/elements/element_0.png
...
```

### 问题：部分图片显示，部分不显示

- 设置 `RENDER_MODE = 'auto'`，未加载成功的会自动回退到颜色模式
- 检查特定图片的路径和文件是否存在

### 问题：想临时切换回颜色模式

修改 `src/constants.ts`：

```typescript
export const RENDER_MODE: RenderMode = 'color';
```

---

## 技术细节

### 加载流程

```
MainScene.preload()
  └─> 读取 ELEMENT_CONFIGS
      └─> 调用 this.load.image(texture, imagePath)
          └─> Phaser 加载图片资源

MainScene.create()
  └─> renderBoard()
      └─> createCellVisual(type)
          ├─> 检查纹理是否存在
          ├─> 存在 → 创建 Sprite
          └─> 不存在 → 创建 Graphics（颜色块）
```

### 支持的图片格式

- **PNG**：支持透明背景，推荐
- **JPG**：不支持透明背景
- **SVG**：矢量图形，推荐（无损缩放）

---

## 最佳实践

1. **使用 SVG**：矢量图形可以在任何尺寸下保持清晰
2. **统一风格**：6 种元素保持一致的艺术风格
3. **透明背景**：使用 PNG 或 SVG 的透明背景
4. **优化大小**：每张图片建议不超过 50KB
5. **使用 auto 模式**：在开发时使用 `'auto'` 模式，更灵活

---

## 示例资源

项目自带的 SVG 图片：

- **element_0.svg** - 红色圆形
- **element_1.svg** - 青色方形
- **element_2.svg** - 黄色星形
- **element_3.svg** - 绿色三角形
- **element_4.svg** - 紫色菱形
- **element_5.svg** - 橙色六边形

这些可以作为参考模板，你可以：
- 直接使用
- 修改颜色和形状
- 完全替换为自己的设计

---

## 快速开始

### 选项 A：使用默认图片

```bash
# 什么都不用改，直接运行
npm run dev
```

### 选项 B：切换回纯颜色

修改 `src/constants.ts`：
```typescript
export const RENDER_MODE: RenderMode = 'color';
```

### 选项 C：使用自己的图片

1. 准备 6 张图片（PNG/JPG/SVG）
2. 放到 `public/assets/elements/` 目录
3. 修改 `ELEMENT_CONFIGS` 中的 `imagePath`
4. 运行 `npm run dev`

---

**祝你玩得开心！** 🎮
